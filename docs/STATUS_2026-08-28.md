# Fight AI Web — Cierre de jornada 2026-08-28

## Rama actual

`web/mvp` en `pinoaraj/fight-ai`.

## Stack

- Next.js 15.5.24
- React 19
- TypeScript
- Docker
- GitHub Actions
- AWS ECR
- AWS ECS/Fargate
- Application Load Balancer
- Gemini server-side

## Implementado

- carga y reproducción local de video;
- selección básica del peleador objetivo;
- selección boxing/kickboxing;
- stance;
- análisis vía backend compartido cuando `FIGHT_AI_API_URL` existe;
- fallback Gemini autenticado desde servidor;
- provider attribution con requisito `usedInReport=true`;
- fortalezas, prioridades, análisis del rival, plan táctico y drills;
- evidencia con timestamps clickeables;
- `/api/health`;
- CI con typecheck, production build, runtime smoke y Docker build;
- deploy automático con GitHub OIDC;
- ECR privado;
- ECS Fargate;
- ALB público;
- prueba desplegada de Gemini usando clip de guantes rojos.

## Endpoint beta actual

`http://fight-ai-web-alb-2053895073.sa-east-1.elb.amazonaws.com`

Antes de exposición general falta HTTPS.

## Infra actual

- AWS account: 379549361550
- ECR region: `us-east-2`
- runtime region: `sa-east-1`
- ECR repository: `fight-ai-web`
- ECS cluster/service/task family: `fight-ai-web`
- GitHub deploy role: `FightAIGitHubDeployRole`
- ECS execution role: `FightAIEcsTaskExecutionRole`

## Seguridad

- no hay AWS static keys en el repo;
- GitHub usa OIDC;
- Gemini se inyecta server-side en ECS;
- la key no llega al navegador;
- Secrets Manager/SSM aún no están disponibles para esta cuenta y deben reemplazar el mecanismo beta actual cuando sea posible.

## Pendientes prioritarios

1. Separar esta rama a `pinoaraj/fight-ai-web`.
2. Actualizar el trust OIDC de AWS para el subject del nuevo repositorio.
3. Completar paridad de flujo/UI con Android.
4. Selección/re-identificación visual del peleador equivalente a Android.
5. Estado de procesamiento asíncrono.
6. Upload privado y recuperable para videos grandes.
7. Backend CV/Pose compartido.
8. Visual Coach con correcciones gráficas específicas.
9. PDF con la misma jerarquía del reporte Android.
10. HTTPS con ACM.
11. Logging/observabilidad.
12. E2E completo con video real grande.

## Regla de producto

Android sigue siendo la fuente de verdad para flujo, inputs, jerarquía del reporte, provider status, drills, evidencia, Visual Coach y exportación.

## Punto para retomar

Primero completar la separación del repo y el OIDC. Después continuar la paridad Android y el pipeline asíncrono sin rehacer el MVP actual.
